services:
  clonehero-server:
    build: ./Docker
    container_name: clonehero-server
    env_file:
      - server.env
    ports:
      - "14242:14242/udp"
    volumes:
      - ./data:/data
    init: true