services:
  clonehero-server:
    image: ghcr.io/funkecoder23/clonehero-server-docker:latest
    build: ./Docker
    container_name: clonehero-server
    env_file:
      - server.env
    ports:
      - "14242:14242/udp"
    volumes:
      - ./data:/data
    init: true